subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'

    group = 'myhttp'
    version = '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }

    dependencies {
        testImplementation 'org.junit.jupiter:junit-jupiter:5.8.2'
    }

    tasks.test {
        useJUnitPlatform()
    }
}